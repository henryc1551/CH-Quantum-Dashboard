{
  "tasks": {
    "dev": "deno run --allow-net --allow-read --allow-env --watch server.js",
    "fmt": "deno fmt",
    "lint": "deno lint"
  },
  "fmt": { "useTabs": false, "lineWidth": 100, "indentWidth": 2 },
  "lint": { "rules": { "include": ["recommended"] } }
}
